package com.prime.datastructure.sysdata;

public enum EnTagType {
    // Lock, not support as filter
    LOCK,
    // Hide, when as filter,not show hide channel
    HIDE,
    // delete, not support as filter
    DEL,
}
