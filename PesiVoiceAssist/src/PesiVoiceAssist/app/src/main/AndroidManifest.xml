<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system">

    <permission android:name = "com.pesi.voice_assist.start"/>
    <permission android:name = "com.pesi.voice_assist.close"/>
    <permission android:name = "com.pesi.voice_assist.close.mic"/>

    <uses-permission android:name = "com.pesi.remote_service.server.response"/>
    <uses-permission android:name = "com.pesi.remote_service.server.response.text"/>
    <uses-permission android:name = "com.pesi.remote_service.send.mic.open"/>
    <uses-permission android:name = "com.pesi.remote_service.send.mic.close"/>
    <uses-permission android:name = "com.pesi.remote_service.send.receive.audio.data"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/transparent_activity"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>